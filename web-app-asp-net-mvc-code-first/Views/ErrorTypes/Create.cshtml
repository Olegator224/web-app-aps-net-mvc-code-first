@using web_app_asp_net_mvc_code_first.Models.Entities
@model ErrorType

<h3>Тип ошибки</h3>

@using (Html.BeginForm("Create", "ErrorTypes", FormMethod.Post, new { @enctype = "multipart/form-data" }))
{
    @Html.EditorForModel()
    <input type="submit" value="Сохранить" />
    @Html.ActionLink("Отменить", "Index", "ErrorTypes")
}